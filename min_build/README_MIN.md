# Floating Clipper — MIN build (no WebSocket)

Windows + macOS. Overlay die elke 300ms de actieve tab/window ophaalt — zonder bridge of extensie.

## Start
- Windows: `start.cmd`
- macOS: `start.command`
